draw_config:
  svg_extra_style: >
    svg.keymap {
      font-size: 12px;
    }
    text.combo, text.hold, text.shifted {
      font-size: 10px;
    }
parse_config:
  raw_binding_map: { "&kp BACKSPACE": "$$tabler:backspace$$" }
